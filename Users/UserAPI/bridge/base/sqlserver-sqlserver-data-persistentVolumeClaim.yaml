#! sqlserver-sqlserver-data-persistentVolumeClaim.yaml
# Generated code, do not edit
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: sqlserver-sqlserver-data
    namespace: taskapi
    labels:
        com.docker.compose.service: sqlserver
        com.docker.compose.volume: sqlserver_data
spec:
    accessModes:
        # TODO would need to check which services use this volume
        - ReadWriteOnce
    volumeMode: Filesystem
    resources:
        requests:
            storage: 100Mi
